import 'package:flutter/material.dart';

class RedemptionFailedScreen extends StatelessWidget {
  const RedemptionFailedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement redemption failure state with reason and retry option
    return Scaffold(
      appBar: AppBar(title: const Text('Redemption failed')),
      body: const Center(child: Text('TODO: Redemption failed screen')),
    );
  }
}
