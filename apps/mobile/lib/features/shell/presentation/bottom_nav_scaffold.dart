export 'app_shell.dart';
// bottom_nav_scaffold.dart is merged into app_shell.dart for this shell.
// Add a separate BottomNavScaffold widget here if custom bottom nav
// behaviour is needed (e.g. hiding nav bar on scroll).
