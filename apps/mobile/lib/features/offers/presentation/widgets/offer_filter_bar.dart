import 'package:flutter/material.dart';

// TODO: implement filter bar (distance, category, sort)
class OfferFilterBar extends StatelessWidget {
  const OfferFilterBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 48, child: Placeholder());
  }
}
